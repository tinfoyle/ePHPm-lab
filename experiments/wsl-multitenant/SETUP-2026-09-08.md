# Setup record — 2026-09-08

**Environment ready; security testing has not started.**

- WSL registration: `EPHPM-Lab` (new Ubuntu Base 24.04.4 amd64 import).
- Runtime: ePHPm `0.10.1`, PHP `8.4.23`; archive SHA-256 matched `versions.json`.
- Kernel observed: `6.18.33.2-microsoft-standard-WSL2`.
- PID 1: systemd; cgroups: v2; `/sys/kernel/btf/vmlinux`: present.
- `ephpm-lab.service`: active/running, four PHP execution threads configured.
- Effective systemd limits: 2,147,483,648 bytes memory, 200% CPU, 256 tasks.
- Both `alice.lab.test` and `bob.lab.test` returned their own readiness identity with native KV and DB functions available.
- Reused `/hello.php` returned the existing fixture's `route=hello`, `message=ok` response. CPU and JSON routes are staged; no load run.
- The service cgroup showed `vhost_bind4`, `vhost_bind6`, `vhost_connect4`, and `vhost_connect6` attachments. Setup asserts all four names are present.
- The service network namespace had only loopback and local routes. Its nftables input/output chains default to drop and allow loopback for the ePHPm BPF handoff.
- Windows drive automount and executable interop are disabled. No Windows drive mount was observed; WSL's read-only driver integration mount remains part of the WSL environment.
- Clean import/provisioning and a subsequent scripted `-Resume` completed. Resume transferred its payload through binary stdin with drive mounting and interop disabled.
- PowerShell parsing, shell syntax and Git whitespace checks passed.

## Local environment caveat

WSL reported a root **user-session** startup warning. The journal identifies an inotify allocation failure (`Too many open files`) in `user@0.service`; unrelated system password-watch units also failed, and `kmod-static-nodes.service` failed. The **system** manager and ePHPm system service are running. No shared kernel sysctls or other WSL distributions were changed to suppress these warnings. Treat the shared WSL resource environment as a limitation to address before stress testing, particularly any resource-exhaustion claims.

Setup corrected Ubuntu's bpftool packaging path for WSL and removed dependence on the WSL UNC share. A hidden idle WSL client keeps this guest alive after provisioning. Its PID is in the ignored host cache.

## What this does not establish

No cross-tenant file, cache, database, session, network or process isolation claim has been tested. Databases are configured for lazy per-site creation, not populated with application data. Landlock enforcement, preview builds, GitHub integration, DNS/public egress, TLS/HTTP2 and adversarial behavior remain for the next plan. WordPress and k6 assets are staged only.

Guest inventory: `/var/lib/ephpm-lab/setup-status.txt`, `packages.tsv`, and `versions.json`. Generated canaries stay in the guest; they are not copied into this report or the repository.
